name: Generate ubuntu builder images
# Generates an image that encapsulate an environment on which it is possible to build psibase.

env:
  GITHUB_OUTPUT: ""
  # Pin to a psibase ref that includes build.sh skip-pull-when-local (PR #1988).
  # Switch to v0.26.0-pre once that release lands.
  PSIBASE_VALIDATION_REF: a2af0faef9486805715f87d529c37db751de3e48

on:
  workflow_call:
    inputs:
      ubuntu_version:
        description: "On what version of ubuntu should the build run?"
        type: string
        required: true
        default: "2204"

jobs:
  ubuntu-builder:
    name: psibase-builder-ubuntu-${{ inputs.ubuntu_version }}
    strategy:
      matrix:
        runner: [ubuntu-latest, arm-runner-2]
        include:
          - runner: ubuntu-latest
            platform: "linux/amd64"
            platform_short: "amd64"
          - runner: arm-runner-2
            platform: "linux/arm64"
            platform_short: "arm64"
    runs-on: ${{ matrix.runner }}

    steps:
      - name: (PR only) - Maximize build space
        if: ${{ github.event_name == 'pull_request' && matrix.runner == 'ubuntu-latest' }}
        uses: James-Mart/maximize-build-space@master
        with:
          root-reserve-mb: 24576
          temp-reserve-mb: 512
          remove-android: "true"

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0

      - name: Preparation
        id: prep
        run: |
          IMAGE="psibase-builder-ubuntu-${{ inputs.ubuntu_version }}"
          echo "image=${IMAGE,,}" >> $GITHUB_OUTPUT

      - name: Building ${{ matrix.platform_short }}
        run: true

      - name: Config docker buildx network
        uses: docker/setup-buildx-action@v3

      - name: Login in to registry
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Publish Image
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          file: docker/ubuntu-${{ inputs.ubuntu_version }}-builder.Dockerfile
          tags: "ghcr.io/${{ github.repository_owner }}/${{ steps.prep.outputs.image }}:${{ github.sha }}-${{ matrix.platform_short }}"
          platforms: ${{ matrix.platform }}
          provenance: false
          outputs: type=image
    
      - name: (PR Only) - Build image archive
        id: archive
        if: ${{ github.event_name == 'pull_request' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/ubuntu-${{ inputs.ubuntu_version }}-builder.Dockerfile
          tags: "ghcr.io/${{ github.repository_owner }}/${{ steps.prep.outputs.image }}:${{ github.sha }}-${{ matrix.platform_short }}"
          platforms: ${{ matrix.platform }}
          outputs: type=docker,dest=${{ steps.prep.outputs.image }}-${{ matrix.platform_short }}-image.tar

      - name: (PR only) - Upload image archive as artifact
        if: ${{ github.event_name == 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prep.outputs.image }}-${{ matrix.platform_short }}-image
          path: ${{ steps.prep.outputs.image }}-${{ matrix.platform_short }}-image.tar
          retention-days: 1

      - name: (PR only) - Load builder image
        if: ${{ github.event_name == 'pull_request' && matrix.platform_short == 'amd64' }}
        run: docker load -i ${{ steps.prep.outputs.image }}-${{ matrix.platform_short }}-image.tar

      - name: (PR only) - Checkout psibase
        if: ${{ github.event_name == 'pull_request' && matrix.platform_short == 'amd64' }}
        uses: actions/checkout@v4
        with:
          repository: gofractally/psibase
          ref: ${{ env.PSIBASE_VALIDATION_REF }}
          path: psibase
          submodules: recursive
          fetch-depth: 0

      - name: (PR only) - Validate with psibase build
        if: ${{ github.event_name == 'pull_request' && matrix.platform_short == 'amd64' }}
        working-directory: psibase
        run: >
          bash .github/scripts/build.sh "$PWD"
          "ghcr.io/${{ github.repository_owner }}/${{ steps.prep.outputs.image }}:${{ github.sha }}-${{ matrix.platform_short }}"
          "${{ inputs.ubuntu_version }}"

  merge: 
    name: merge multi-platform images
    if: ${{ github.event_name != 'pull_request' }}
    runs-on: ubuntu-latest
    needs: ubuntu-builder
    steps:
      - name: Config docker buildx
        uses: docker/setup-buildx-action@v3

      - name: Preparation
        id: prep
        run: |
          IMAGE="psibase-builder-ubuntu-${{ inputs.ubuntu_version }}"
          echo "image=${IMAGE,,}" >> $GITHUB_OUTPUT

      - name: Login in to registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push manifest
        run: |
          IMAGE="ghcr.io/${{ github.repository_owner }}/${{ steps.prep.outputs.image }}:${{ github.sha }}"
          docker manifest create ${IMAGE} \
            --amend ${IMAGE}-arm64 \
            --amend ${IMAGE}-amd64
          docker manifest push ${IMAGE}